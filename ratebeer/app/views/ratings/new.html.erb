<h2>Create new rating</h2>


<%= form_for(@rating) do |f| %>
  beer id:
   <%= f.select :beer_id,
   # 							Get from model, what to send, what to show
  	options_from_collection_for_select(@beers, :id, :to_s)
   %>
  score: <%= f.number_field :score %>
  <%= f.submit %>
<% end %>